{
  "name": "spending-analyzer-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "dev:tauri": "turbo run dev --filter=@spending-analyzer/tauri",
    "dev:web": "turbo run dev --filter=@spending-analyzer/web",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
